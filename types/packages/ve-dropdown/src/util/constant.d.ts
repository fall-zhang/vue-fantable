export const PREFIX_CLS: "ve-dropdown-";
export namespace EMIT_EVENTS {
    let FILTER_RESET: string;
    let FILTER_CONFIRM: string;
    let VISIBLE_CHANGE: string;
    let ITEM_SELECT_CHANGE: string;
}
export namespace COMPS_NAME {
    let VE_DROPDOWN: string;
}
